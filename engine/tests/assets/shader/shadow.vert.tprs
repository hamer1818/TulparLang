// kaynak-ozet: 7955a836cc95  (engine/rhi/shaders/shadow.vert)
// Golge haritasi: yalniz derinlik. G3 (sabit boy dizi) kullanir — bu dosya
// bugunku Tulpar gramerinden GECMEZ, alt kumeden gecer.
str asama = "vert";

vec3 in_pos = girdi(0);

struct Frame {
  mat4 viewproj;
  mat4 view;
  mat4[3] light_viewproj; // golge kademeleri (Renderer::kMaxCascades)
  vec4 light_dir;
  vec4 ambient;
  vec4 shadow_params;
  vec4 cluster_params;
  uvec4 cluster_grid;
  vec4 cascade_params;
}
Frame u = tekduze(0, 0);

struct Push { mat4 model; vec4 color; uvec4 skin; } // skin.y = kademe
Push pc = itme();

func main() { gl_Position = u.light_viewproj[pc.skin.y] * pc.model * vec4(in_pos, 1.0); }
