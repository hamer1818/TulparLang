// Foreach (for..in) örneği

print("=== Foreach ile carpim tablosu ===");

for (i in range(10)) {
    int sonuc = i * 2;
    print(i, "x 2 =", sonuc);
}

print("");
print("=== Foreach ile kare alma ===");

for (n in range(5)) {
    int kare = n * n;
    print(n, "^ 2 =", kare);
}

