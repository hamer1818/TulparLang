func tak(int x, int y, int z): int {
    if (y >= x) { return z; }
    return tak(tak(x - 1, y, z), tak(y - 1, z, x), tak(z - 1, x, y));
}
int n = toInt(env("BENCH_N"));
if (n <= 0) { n = 18; }
print(tak(n, n / 2, 0));
