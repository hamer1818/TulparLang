// Native typed AOT: explicit `: int` return type triggers unboxed
// LLVM-native i64 codegen path (no vm_binary_op, direct LLVM add).
func fibonacci(int n): int {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

print(fibonacci(35));
